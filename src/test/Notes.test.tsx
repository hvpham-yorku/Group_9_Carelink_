/**
 * @vitest-environment jsdom
 * Integration tests for the Notes page:
 * - render/empty state
 * - create note + localStorage persistence
 */

import { describe, it } from "vitest";

describe("Notes page", () => {
  it("placeholder test", () => {});
});